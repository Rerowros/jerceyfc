Вот краткая и практичная инструкция (Cheatsheet) по написанию **MDX** для твоего проекта.

Сохрани её себе в заметки. Главное отличие MDX от обычного Markdown — **строгость синтаксиса** и **компоненты**.

---

## 📝 Структура файла `.mdx`

Файл состоит из трех частей:

1.  **Frontmatter** (метаданные между `---`).
2.  **Импорты** (если нужны дополнительные компоненты).
3.  **Контент**, разделенный на языки через компонент `<Lang>`.

### Шаблон файла

```mdx
---
title:
  ru: 'Название проекта'
  en: 'Project Title'
description:
  ru: 'Краткое описание для SEO и превью.'
  en: 'Short description for SEO and preview.'
tags: ['Next.js', 'TypeScript']
pubDate: 2024-03-20
type: 'web'
---

import MyChart from '../../components/charts/MyChart.astro';

<Lang val="ru">
# Заголовок H1 (обычно не нужен, так как берется из title)

Здесь идет текст на русском. Можно использовать **жирный** или _курсив_.

### Особенности

- Фича 1
- Фича 2

**Важно:** После списка нужна пустая строка перед закрытием тега!

</Lang>

<Lang val="en">
Here goes English text.

### Features

- Feature 1
- Feature 2

</Lang>
```

---

## ⚠️ "Золотые правила" MDX (Чтобы не было ошибок)

### 1. Правило пустой строки (Критично!)

MDX парсер путается, если вы вкладываете Markdown (списки, цитаты) внутрь JSX тегов без отступов.

❌ **ОШИБКА:**

```mdx
<Lang val="ru">- Пункт 1 - Пункт 2</Lang>
```

_Ошибка: `Expected the closing tag </Lang>...`_

✅ **ПРАВИЛЬНО:**

```mdx
<Lang val="ru">
- Пункт 1
- Пункт 2

</Lang>
```

_(Всегда оставляй пустую строку перед закрывающим тегом компонента, если внутри был список или заголовок)_.

### 2. Фигурные скобки `{}` — это код

В MDX всё, что внутри `{ ... }`, считается JavaScript-выражением.

- Если хочешь написать просто скобки в тексте, используй бэкслеш: `\{ object \}`.
- Или код-блок: `` `const a = { b: 1 }` ``.

---

## 🎨 Возможности форматирования

### Стандартный Markdown (работает как обычно)

| Элемент          | Синтаксис    | Пример               |
| :--------------- | :----------- | :------------------- |
| **Заголовки**    | `##`, `###`  | `### Технологии`     |
| **Жирный**       | `**текст**`  | **текст**            |
| **Курсив**       | `_текст_`    | _текст_              |
| **Списки**       | `-` или `1.` | - Пункт 1            |
| **Цитаты**       | `> Текст`    | > Цитата             |
| **Код (инлайн)** | `` `код` ``  | `const x = 1`        |
| **Разделитель**  | `---`        | Горизонтальная линия |

### Продвинутый Markdown (GFM)

У тебя подключен плагин `remark-gfm`, поэтому работают таблицы и чекбоксы.

**Таблицы:**

```md
| Технология | Версия |
| :--------- | :----- |
| Astro      | 5.0    |
| React      | 18     |
```

**Чекбоксы:**

```md
- [x] Сделано
- [ ] В планах
```

**Код с подсветкой:**

```js
// Укажи язык после тройных кавычек
console.log('Hello World');
```

---

## 🧩 Вставка Компонентов (Сила MDX)

Ты можешь импортировать и вставлять любые компоненты (`.astro`, `.jsx`, `.tsx`) прямо в текст.

**Пример:** Вставить интерактивный график или кнопку.

```mdx
---

...
---

import { Image } from 'astro:assets';
import myImage from '../../assets/project/demo.png';
import DemoButton from '../../components/ui/DemoButton.jsx';

<Lang val="ru">
  Вот так выглядит интерфейс:
  <Image src={myImage} alt="Скриншот" class="rounded-xl shadow-lg" />
  Попробуйте сами:
  <div class="my-4">
    <DemoButton client:visible>Нажать меня</DemoButton>
  </div>
</Lang>
```

### Использование CSS классов (Tailwind)

В MDX можно писать обычный HTML с классами Tailwind.
Вместо `class` в чистом React нужно писать `className`, но в MDX внутри Astro обычно работает и `class`.

```html
<div class="rounded-lg border border-blue-500 bg-blue-500/10 p-4 text-blue-200">
  💡 **Совет:** Используйте этот блок для важных заметок.
</div>
```
