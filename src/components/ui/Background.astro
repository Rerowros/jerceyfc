<div class="site-background" aria-hidden="true">
  <div class="ambient-shape ambient-shape-one"></div>
  <div class="ambient-shape ambient-shape-two"></div>
  <div class="ambient-dots"></div>
</div>

<style>
  .site-background {
    position: fixed;
    inset: 0;
    z-index: -1;
    overflow: hidden;
    pointer-events: none;
    background:
      radial-gradient(circle at 12% 10%, color-mix(in srgb, var(--color-primary) 14%, transparent), transparent 28rem),
      radial-gradient(circle at 88% 46%, color-mix(in srgb, var(--color-primary) 9%, transparent), transparent 32rem),
      var(--color-background);
  }

  .ambient-shape {
    position: absolute;
    width: 15rem;
    aspect-ratio: 1;
    border-radius: 40% 60% 65% 35% / 45% 35% 65% 55%;
    background: radial-gradient(circle at 35% 30%, color-mix(in srgb, var(--color-primary) 42%, white 10%), transparent 70%);
    opacity: 0.18;
  }

  .ambient-shape-one {
    top: 16%;
    right: -7rem;
    animation: ambient-drift-one 18s ease-in-out infinite alternate;
  }

  .ambient-shape-two {
    bottom: 4%;
    left: -9rem;
    width: 19rem;
    opacity: 0.12;
    animation: ambient-drift-two 22s ease-in-out infinite alternate;
  }

  .ambient-dots {
    position: absolute;
    inset: 0;
    opacity: 0.17;
    background-image: radial-gradient(circle, var(--color-muted-foreground) 1px, transparent 1px);
    background-size: 26px 26px;
    mask-image: linear-gradient(to bottom, transparent, black 18%, black 82%, transparent);
  }

  @keyframes ambient-drift-one {
    to { transform: translate3d(-26px, 34px, 0) rotate(10deg); }
  }

  @keyframes ambient-drift-two {
    to { transform: translate3d(34px, -24px, 0) rotate(-12deg); }
  }

  :global(html.light) .site-background {
    background:
      radial-gradient(circle at 12% 10%, color-mix(in srgb, var(--color-primary) 12%, transparent), transparent 28rem),
      radial-gradient(circle at 88% 46%, color-mix(in srgb, var(--color-primary) 8%, transparent), transparent 32rem),
      var(--color-background);
  }

  @media (prefers-reduced-motion: reduce) {
    .ambient-shape { animation: none; }
  }
</style>
